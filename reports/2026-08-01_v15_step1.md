# Version 1.5 step 1 — coding foundation

**Date:** 2026-08-01

## Result

- Added `edit_file` for atomic exact single-match replacement inside the same
  resolved workspace boundary as the other filesystem tools.
- `edit_file` is destructive and retains the existing checkpointed explicit
  confirmation flow.
- Tool arguments are checked against their advertised JSON Schema before a
  destructive call can reach confirmation and again before execution.
- The Version 1.5 coding profile raises only the completion cap from 512 to
  `MODEL_MAX_TOKENS=4096`; the running server remained at its validated 16k
  context and was not restarted.

## Checks

- Full offline suite: `228 passed in 4.52s`.
- `git diff --check`: passed; Git emitted only an existing line-ending warning
  for `tests/test_agent_graph.py`.
- Live coding request against the already running Gemma endpoint with a 4096
  cap: 56 input tokens, 1215 output tokens, 4258 characters, 19.83 seconds,
  `finish_reason=stop`. The response contained HTML, canvas and keyboard code.
  This checks the profile, not whether the generated game works.
- Live agent edit smoke: Gemma selected `edit_file` with all three valid
  arguments; the sandbox file was unchanged before approval and changed from
  `status=old` to `status=new` after approval; the resumed turn returned a tool
  result and final assistant message.
- Restarted only the Chainlit UI so the running product loaded the new tool and
  profile; `http://127.0.0.1:8100` returned HTTP 200. The vLLM process was not
  restarted.

## Boundary

No server configuration, model weights, persistent product database, remote or
GPU reservation setting changed. The bounded task graph, task-scoped grant and
static/browser verification remain later Version 1.5 steps.
